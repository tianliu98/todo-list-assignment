{
  "name": "todo-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --watch ./db/db.json",
    "live-server": "live-server --ignore='./db' ",
    "start": "npm run json:server & npm run live-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.2",
    "live-server": "^1.2.2"
  }
}
